Small command line program for manually logging in a project.

```
git clone https://github.com/eurdoch/projlog.git
cd projlog
cargo install --path .
```

```
cd project-folder
projlog A log entry
```
will append timestamped log to local file `plog`
